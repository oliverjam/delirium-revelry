{
  "scripts": {
    "dev": "parcel serve 'pages/**/*.html' --dist-dir _site",
    "build": "parcel build 'pages/**/*.html' --dist-dir _site"
  },
  "devDependencies": {
    "parcel": "^2.8.2",
    "posthtml-include": "^1.7.4",
    "stylelint": "^14.14.0",
    "stylelint-config-recommended": "^9.0.0"
  },
  "stylelint": {
    "extends": "stylelint-config-recommended"
  }
}
